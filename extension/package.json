{
  "name": "@internship-agent/extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../scripts/write-build-info.mjs && npm run clean && vite build && vite build --config vite.content.config.ts",
    "dev": "node ../scripts/write-build-info.mjs && npm run clean && concurrently -n app,content -c cyan,magenta \"vite build --watch\" \"vite build --watch --config vite.content.config.ts\"",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@internship-agent/shared": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "vite": "^6.0.7"
  }
}
